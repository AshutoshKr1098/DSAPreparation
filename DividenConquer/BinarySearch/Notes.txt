1. While calculating binary search we always reduce the search space to half. In this way the 
time complexity of binary search is O(logn)
2. To avoid integer overflow, a good way to calculate mid is using mid = s + (e-s)/2;
